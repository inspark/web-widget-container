
import {environment as env} from '../environments/environment';



export function transformChartUrlConfig(id: number, isCalc, isSignal = false): string {
  return '';
}



export const environment = env;
